{
  "private": true,
  "devDependencies": {
    "husky": "1.0.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "sbt -Dfile.encoding=UTF8 prepare && git add --all",
      "pre-push": "sbt -Dfile.encoding=UTF8 check"
    }
  }
}
